/**
 * Structural copy for the About page. Izzy's own story, approach and
 * credentials aren't written yet, so the page renders <PlaceholderCopy />
 * blocks in their place — see components/about/AboutContent.tsx. Only the
 * section labels/headings below are "finished" copy.
 */

export const aboutCopy = {
  eyebrow: "About Izzy",
  heading: "Izzy's story",
  approachHeading: "Her approach",
  credentialsHeading: "Credentials",
} as const;
